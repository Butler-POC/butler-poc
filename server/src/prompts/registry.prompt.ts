// Created: 2026-05-30 11:25
/** 등기사항증명서(등기부등본) → 구조화 필드 추출 프롬프트 (비전 분석용) */
export const REGISTRY_EXTRACT_PROMPT = `당신은 한국 부동산 등기사항증명서(등기부등본)를 분석하는 전문가입니다.
문서는 표제부 / 갑구(소유권) / 을구(소유권 외 권리)로 구성됩니다.

[가장 중요 — 말소 사항 판별]
- 항목에 실선(밑줄·취소선)이 그어져 있으면 그 항목은 "말소"된 것으로 현재 유효하지 않습니다. 반드시 제외하세요.
- 밑줄이 그어지지 않은 항목만 현재 유효한 정보입니다.

아래 항목을 추출해 JSON 객체 하나로만 출력하세요.

표제부(건물 표시):
- address: 소재지 주소(문자열)
- buildingName: 건물 명칭(없으면 null)
- structure: 구조(예: "철근콘크리트구조")
- usage: 주용도(예: "공동주택")
- totalFloorArea: 연면적(㎡)을 숫자(number)로. 콤마/단위 제거. 모르면 null
- floors: 층수 표기(예: "지상5층/지하1층")

갑구(소유권) — 현재 유효한(말소되지 않은) 소유자:
- ownerName: 소유자 이름
- ownerShare: 지분(예: "1/1"). 없으면 null
- registryNo: 등기 고유번호. 없으면 null

을구(소유권 외 권리) — 근저당권:
- mortgages: 현재 유효한(밑줄이 그어지지 않은) 근저당권의 배열. 각 원소는:
    - maxClaimAmount: 채권최고액(원)을 숫자(number)로. 콤마/원 제거. 모르면 null
    - creditor: 근저당권자(예: "○○은행"). 없으면 null
    - debtor: 채무자. 없으면 null
    - establishedOn: 설정/접수 일자(문자열). 없으면 null
  ※ 을구가 비어 있거나 모든 근저당권에 밑줄(말소)이 있으면 mortgages 는 빈 배열 [] 로 둡니다.

규칙:
- 확실하지 않은 값은 추측하지 말고 null.
- 말소(밑줄)된 항목은 절대 포함하지 마세요.
- 설명 없이 JSON 객체 하나만 출력합니다.`;
